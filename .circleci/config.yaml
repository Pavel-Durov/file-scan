version: 2.1
jobs:
  test:
    docker:
      - image: node:12.15-slim
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: yarn
      - run:
          name: Lint
          command: yarn lint
      - run:
          name: Test Coverage
          command: yarn test

workflows:
  version: 2
  test-wf:
    jobs:
      - test  
